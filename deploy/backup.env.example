# Copy to /etc/afterbell/backup.env with mode 600.
# This is an rclone destination, not a local path. `rclone copy` is used so
# remote data is never deleted by a local cleanup.
AFTERBELL_BACKUP_REMOTE=
