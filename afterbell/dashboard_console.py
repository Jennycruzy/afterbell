"""Operational dashboard for AFTERBELL."""
PAGE = r"""<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>AFTERBELL | Market protection</title><style>
:root{color-scheme:dark;--bg:#081015;--panel:#101c22;--raised:#172830;--line:#29414a;--text:#eff7f2;--muted:#91aaa8;--mint:#9cf1c1;--cyan:#7bdff2;--violet:#c1adff;--amber:#f6ca79;--red:#ff8585;--blue:#8dbcf4;--pink:#f2a8d5;--shadow:0 12px 35px #00000025}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 8% -12%,#1a435055,transparent 32rem),radial-gradient(circle at 96% 0%,#3b225133,transparent 30rem),var(--bg);color:var(--text);font:13px/1.5 system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,select,input{font:inherit;color:inherit;background:var(--raised);border:1px solid var(--line);border-radius:6px;padding:7px 10px}button{cursor:pointer;transition:border-color .15s,color .15s,background .15s}button:hover,button.active{border-color:var(--mint);color:var(--mint);background:#1d3b39}a{color:var(--mint);text-underline-offset:3px}header{position:sticky;top:0;z-index:5;background:#0b171cf2;backdrop-filter:blur(13px);border-bottom:1px solid var(--line);padding:12px 18px;display:flex;align-items:center;gap:13px;flex-wrap:wrap}header strong{letter-spacing:.13em;font-size:15px;color:var(--text)}header strong:first-letter{color:var(--cyan)}header small{margin-left:auto;color:var(--muted)}main{max-width:1700px;margin:auto;padding:14px;display:grid;gap:12px}.tag{display:inline-block;font:11px ui-monospace,SFMono-Regular,Menlo,monospace;padding:3px 7px;border:1px solid var(--line);border-radius:5px;color:var(--muted);letter-spacing:.02em}.status{font-weight:650}.status.clear,.status.complete,.status.healthy{color:var(--mint);border-color:#4d9d78;background:#1b4a3a55}.status.caution{color:var(--amber);border-color:#a77a35;background:#5c421e55}.status.blocked,.status.failed,.status.stale{color:var(--red);border-color:#a95050;background:#632d3555}.status.paused{color:var(--violet);border-color:#7560bc;background:#4a3e7b55}.status.neutral{color:var(--muted)}.metrics{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:10px}.panel,.metric{background:linear-gradient(145deg,#14232aee,#0f1a20ee);border:1px solid var(--line);border-radius:9px;min-width:0;box-shadow:var(--shadow)}.metric{position:relative;overflow:hidden;padding:13px;border-top:2px solid var(--accent,var(--line))}.metric:after{content:"";position:absolute;right:-32px;top:-38px;width:100px;height:100px;border-radius:50%;background:var(--accent);opacity:.055;filter:blur(1px)}.metric .value{color:var(--accent)}.metric:nth-child(1){--accent:var(--cyan)}.metric:nth-child(2){--accent:var(--violet)}.metric:nth-child(3){--accent:var(--red)}.metric:nth-child(4){--accent:var(--mint)}.metric:nth-child(5){--accent:var(--amber)}.metric:nth-child(6){--accent:var(--pink)}.label,h2{font-size:10px;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--muted)}.value{font:600 24px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:-.05em;margin:8px 0}.note{font-size:11px;color:var(--muted);overflow-wrap:anywhere}h2{margin:0}.ph{padding:11px 13px;display:flex;gap:9px;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line)}.body{padding:13px}.workspace{display:grid;grid-template-columns:1.45fr 1fr 1fr;gap:12px}.span2{grid-column:span 2}.full{grid-column:1/-1}.stack{display:grid;gap:12px;align-content:start}.row{display:flex;gap:10px;justify-content:space-between;align-items:center;flex-wrap:wrap}.facts{display:grid;grid-template-columns:1fr 1fr;gap:11px}.fact{padding:8px 0;border-bottom:1px solid var(--line)}.fact b{display:block;font:14px ui-monospace,SFMono-Regular,Menlo,monospace;margin-top:5px}.check{display:grid;grid-template-columns:10px 1fr auto;gap:8px;padding:10px 0;border-bottom:1px solid var(--line);align-items:start}.check .dot{width:8px;height:8px;border-radius:50%;background:var(--cyan);margin-top:6px;box-shadow:0 0 12px currentColor}.check details{grid-column:2/-1;color:var(--muted);font-size:11px}.check summary{cursor:pointer}.bar{height:5px;background:#26383f;margin:6px 0;border-radius:4px;overflow:hidden}.bar i{display:block;height:100%;background:linear-gradient(90deg,var(--cyan),var(--mint));border-radius:4px}.callout{border-left:3px solid var(--amber);padding:9px 11px;background:#5c421e22;color:var(--muted);font-size:12px}.callout strong{display:block;color:var(--amber);margin-bottom:3px}.tablewrap{overflow:auto}table{border-collapse:collapse;width:100%;white-space:nowrap;font-size:12px}th,td{text-align:left;padding:10px 12px;border-bottom:1px solid var(--line)}th{font-size:10px;color:var(--muted);font-weight:600}td{font-variant-numeric:tabular-nums}svg{display:block;width:100%;height:180px}.chartlegend{display:flex;gap:18px;font:11px ui-monospace,SFMono-Regular,Menlo,monospace;padding:10px 13px}.blue{color:var(--blue)}.green{color:var(--mint)}.receipt{border-bottom:1px solid var(--line);padding:10px 13px}.receipt summary{cursor:pointer;display:flex;gap:10px;align-items:center;flex-wrap:wrap}.receipt p{font-size:12px}.hash{font:10px ui-monospace,SFMono-Regular,Menlo,monospace;color:var(--muted);overflow-wrap:anywhere}.receipts{max-height:410px;overflow:auto}.console{display:flex;gap:8px;flex-wrap:wrap}.console label{display:grid;gap:4px;color:var(--muted);font-size:11px}.console select{min-width:125px}pre{white-space:pre-wrap;overflow-wrap:anywhere;font:11px/1.6 ui-monospace,SFMono-Regular,Menlo,monospace;color:var(--muted);max-height:330px;overflow:auto}footer{font-size:11px;color:var(--muted);padding:6px}.statusline{padding:9px 12px;border:1px solid var(--line);border-radius:6px;font-size:11px;color:var(--muted);background:#0c171cbb}.links{display:flex;gap:15px;flex-wrap:wrap}@media(max-width:1150px){.metrics{grid-template-columns:repeat(3,1fr)}.workspace{grid-template-columns:1fr 1fr}.span2{grid-column:1/-1}}@media(max-width:650px){.metrics{grid-template-columns:1fr 1fr}.workspace{grid-template-columns:1fr}.span2{grid-column:auto}header small{margin-left:0}.value{font-size:20px}.ph{flex-wrap:wrap}main{padding:10px}.panel,.metric{box-shadow:none}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto;transition:none}}
.hero{padding:22px;background:linear-gradient(135deg,#16323bee,#171e31ee);border:1px solid var(--line);border-radius:10px}.hero h1{margin:0 0 7px;font-size:clamp(24px,4vw,42px);line-height:1.08}.hero>p{max-width:850px;color:#c7d8d4;font-size:15px}.journey{display:grid;grid-template-columns:repeat(3,1fr);gap:9px;margin-top:16px}.journey div{padding:11px;background:#08101588;border:1px solid var(--line);border-radius:7px}.journey b{display:block;color:var(--mint);margin-bottom:3px}.demo-note{border-left-color:var(--cyan);margin-top:12px}.demo-note strong{color:var(--cyan)}@media(max-width:650px){.journey{grid-template-columns:1fr}}
</style><meta name="description" content="AFTERBELL is a deterministic safety agent that limits Binance bStock orders before execution."><meta property="og:title" content="AFTERBELL — the stock sleeps, the token does not"><meta property="og:description" content="Live, deterministic safety limits for AI agents trading Binance bStocks."></head><body><header><strong>◔ AFTERBELL</strong><span class="tag">MARKET PROTECTION</span><span class="tag">LIVE MONITOR</span><span id="connection" class="tag">CONNECTING</span><small id="updated">Waiting for recorded state</small><a href="https://github.com/Jennycruzy/afterbell">Source ↗</a></header><main>
<div class="metrics" id="metrics"></div><div class="statusline" id="freshness">Loading recorded market data…</div>
<section class="hero" aria-labelledby="intro-title"><span class="tag">BINANCE AGENT OS · TRACK A</span><h1 id="intro-title">The stock sleeps. The token doesn't.</h1><p>AFTERBELL is a deterministic safety agent for AI agents trading Binance bStocks. It compares the always-open token with the underlying U.S. market and returns the largest defensible order size before execution.</p>
<div class="journey"><div><b>1 · Observe autonomously</b>Market, reference age, liquidity and venue changes are recorded without a request.</div><div><b>2 · Decide deterministically</b>Seven checks produce a ceiling and a hash-linked receipt. A model cannot raise it.</div><div><b>3 · Hand off safely</b>Public tools advise any agent; the demonstrated governed path binds a supported client to a signed authorization.</div></div>
<div class="callout demo-note"><strong>Why the public result may be $0</strong>Public visitors do not provide authenticated account positions. Missing exposure evidence is intentionally blocked, rather than treated as an empty account. The recorded order path below proves a signed authorization was redeemed for 4.86192 USDT—never more than permitted.</div>
<div class="links" style="margin-top:12px"><a href="#decision-panel">See the live decision ↓</a><a href="#proof">See execution proof ↓</a><a href="https://github.com/Jennycruzy/afterbell#architecture">Understand enforcement ↗</a></div></section>
<div class="workspace">
<section class="panel span2"><div class="ph"><h2>NVDA token · price difference & reference age</h2><div id="ranges"><button data-range="1">1H</button> <button data-range="24">24H</button> <button class="active" data-range="0">ALL</button></div></div><div class="chartlegend"><span class="blue">Price difference · bps</span><span class="green">Reference age · hours</span><span id="points" class="note"></span></div><svg id="chart" viewBox="0 0 900 180" role="img" aria-label="Recorded price difference versus reference age"></svg><div class="body note" id="chartnote"></div></section>
<section class="panel" id="decision-panel"><div class="ph"><h2>Latest decision</h2><span id="decisiontag"></span></div><div class="body" id="decision"></div></section>
<section class="panel"><div class="ph"><h2>Safety checks</h2><span class="note">What was checked</span></div><div class="body" id="checks"></div></section>
<section class="panel"><div class="ph"><h2>Allowed size</h2><span class="note">Smallest safe amount</span></div><div class="body" id="sizing"></div></section>
<section class="panel"><div class="ph"><h2>Account exposure</h2></div><div class="body" id="exposure"></div></section>
<section class="panel span2"><div class="ph"><h2>Noticed without being asked</h2><span class="note">The monitor runs unattended</span></div><div class="body" id="posture"></div></section>
<section class="panel full"><div class="ph"><h2>Market view</h2><span class="note">Recorded public books · regular-hours comparisons</span></div><div class="tablewrap"><table><thead><tr><th>Token / underlying</th><th>Trading</th><th>Token mid</th><th>Reference price</th><th>Difference (bps)</th><th>Half-spread (bps)</th><th>Depth within 1%</th><th>Cost to fill $5k (bps)</th><th>Spread vs normal</th><th>Depth vs normal</th><th>Regular-hours samples</th></tr></thead><tbody id="markets"></tbody></table></div></section>
<section class="panel"><div class="ph"><h2>Data coverage</h2><span id="baselineflag"></span></div><div class="body" id="baselines"></div></section>
<section class="panel"><div class="ph"><h2>Configure order limit</h2><span class="tag">SET IN CONFIG</span></div><div class="body" id="policy"></div></section>
<section class="panel"><div class="ph"><h2>Recording & backup</h2><span id="backuptag"></span></div><div class="body" id="operations"></div></section>
<section class="panel span2"><div class="ph"><h2>Decision explorer</h2><span class="tag">RECORDED RESULTS</span></div><div class="body"><div class="console"><label>Pair<select id="symbol"></select></label><label>Result<select id="outcome"><option value="All">All results</option><option value="Clear">Clear</option><option value="Caution">Caution</option><option value="Smaller size">Smaller size</option><option value="Blocked">Blocked</option></select></label><button id="inspect">Inspect latest matching decision</button><button id="download">Download decision JSON</button></div><div id="inspectresult" class="note" style="margin-top:12px">Inspect the recorded request, its measurements, and the resulting size.</div></div></section>
<section class="panel"><div class="ph"><h2>Safety test results</h2><span class="note">Generated report</span></div><div class="body" id="evidence"></div></section>
<section class="panel span2"><div class="ph"><h2>Decision history</h2><span id="receiptcount" class="note"></span></div><div class="receipts" id="receipts"></div></section>
<section class="panel" id="proof"><div class="ph"><h2>Recorded governed order path</h2></div><div class="body" id="acceptance"></div></section>
<section class="panel full"><div class="ph"><h2>Data report & integrity</h2></div><div class="body"><div class="links"><a href="https://github.com/Jennycruzy/afterbell/blob/main/docs/calibration.md">Data coverage report ↗</a><a href="https://github.com/Jennycruzy/afterbell/blob/main/docs/evaluation.md">Safety test method ↗</a><a href="https://github.com/Jennycruzy/afterbell/blob/main/docs/live-acceptance.md">Recorded acceptance ↗</a></div><details><summary>Recorded data report</summary><pre id="calreport"></pre></details><div id="hashes" class="hash"></div></div></section>
</div><footer>AFTERBELL · Market protection for tokenized equities. Compare the always-open token market with its underlying market and see the largest sensible order size.</footer></main>
<script>
(()=>{'use strict';let state=null,range=0,selected=null;const $=id=>document.getElementById(id),esc=v=>String(v??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),fmt=(v,d=2)=>v==null?'—':Number(v).toLocaleString(undefined,{maximumFractionDigits:d}),statusMeta={'Clear':['Clear','clear'],'Caution':['Caution','caution'],'Smaller size':['Smaller size','caution'],'Blocked':['Blocked','blocked'],'Complete':['Complete','complete'],'Healthy':['Healthy','healthy'],'Failed':['Failed','failed'],'Stale':['Stale','stale'],'Waiting':['Waiting','paused'],'Trading':['Trading','healthy'],'Off':['Off','paused'],'Filled':['Filled','healthy'],'Temporarily stopped':['Temporarily stopped','paused'],'Unknown':['Unknown','neutral']},statusLabel=v=>statusMeta[v]?.[0]||(''+(v??'Unknown')).replaceAll('_',' ').toLowerCase(),badge=v=>{let meta=statusMeta[v]||[statusLabel(v),'neutral'];return '<span class="tag status '+meta[1]+'">'+esc(meta[0])+'</span>'},fact=(k,v)=>'<div class="fact"><span class="label">'+esc(k)+'</span><b>'+esc(v)+'</b></div>',time=v=>{let t=Date.parse(v);return Number.isFinite(t)?new Date(t).toLocaleString():'Unknown'},seconds=v=>{let t=Date.parse(v);return Number.isFinite(t)?Math.max(0,(Date.now()-t)/1000):Infinity},age=s=>!Number.isFinite(s)?'Unknown':Math.floor(s/3600)+'h '+Math.floor(s%3600/60)+'m',checkLabels=['Market timing','Liquidity','Price agreement','Corporate actions','Instrument identity','Contract address','Account exposure'],prettyState=v=>v||'Unknown',checkKeys=g=>g?.checks||[],checkName=check=>check?.name||'Safety check',cleanText=value=>String(value??'—'),prettyMeasurement=v=>v||'Unknown';
function draw(){let h=(state.history||[]).filter(x=>Number.isFinite(Number(x.basis_bps))&&Number.isFinite(Number(x.reference_age_s)));if(range&&h.length){let end=Date.parse(h[h.length-1].ts);h=h.filter(x=>Date.parse(x.ts)>=end-range*3600000)}$('points').textContent=h.length+' paired samples';if(h.length<2){$('chart').innerHTML='<text x="450" y="90" text-anchor="middle" fill="#91aaa8">Insufficient paired samples in this window</text>';return}const ts=h.map(x=>Date.parse(x.ts)),start=Math.min(...ts),end=Math.max(...ts);let out='';for(let y of [20,60,100,140])out+='<line x1="55" x2="845" y1="'+y+'" y2="'+y+'" stroke="#29414a"/>';for(const [key,col,div] of [['basis_bps','#8dbcf4',1],['reference_age_s','#9cf1c1',3600]]){let values=h.map(x=>x[key]/div),lo=Math.min(...values),hi=Math.max(...values);let dots=values.map((v,i)=>{let x=55+790*(ts[i]-start)/(end-start||1),y=140-120*(v-lo)/(hi-lo||1);return '<circle cx="'+x+'" cy="'+y+'" r="2" fill="'+col+'"><title>'+esc(time(h[i].ts))+' · '+fmt(v)+(div===1?' bps':' hours')+'</title></circle>'}).join('');out+=dots+'<text x="'+(div===1?2:850)+'" y="15" fill="'+col+'" font-size="10">'+fmt(hi,1)+'</text>'}out+='<text x="55" y="170" fill="#91aaa8" font-size="10">'+esc(time(h[0].ts))+'</text><text x="845" y="170" text-anchor="end" fill="#91aaa8" font-size="10">'+esc(time(h[h.length-1].ts))+'</text>';$('chart').innerHTML=out;$('chartnote').textContent='Separate scales · timestamp-positioned observations · gaps are not interpolated. Hover a point for its value'}
function matching(){return(state.receipts||[]).filter(r=>($('symbol').value==='All'||r.symbol===$('symbol').value)&&($('outcome').value==='All'||r.decision===$('outcome').value))}
function receipts(){let rows=matching();$('receiptcount').textContent=rows.length+' of '+(state.receipts||[]).length+' loaded';$('receipts').innerHTML=rows.map(r=>'<details class="receipt"><summary>'+badge(r.decision)+'<b>'+esc(r.symbol)+'</b><span class="note">#'+esc(r.seq)+' · '+esc(time(r.ts))+' · $'+fmt(r.requested?.notional)+' → $'+fmt(r.allowed_notional)+'</span></summary><p>'+cleanText(r.rationale)+'</p><div class="hash">Decision hash '+esc(r.hash)+'<br>Previous '+esc(r.prev_hash)+'</div></details>').join('')||'<div class="body note">No matching decisions in the loaded window.</div>'}
function freshness(){if(!state)return;let s=seconds(state.guard?.ts),archive=seconds(state.generated_at),poll=seconds(state.status_generated_at||state.generated_at);$('connection').textContent=s>180?'CHECK TIME':'LIVE DATA';$('connection').className='tag '+(s>180?'status stale':'status healthy');$('updated').textContent='Decision '+age(s)+' ago · status checked '+age(poll)+' ago';$('freshness').textContent='Live status checked '+time(state.status_generated_at||state.generated_at)+'. Latest decision recorded '+time(state.guard?.ts)+'. Archive snapshot generated '+time(state.generated_at)+' ('+age(archive)+' ago). '+(state.build_status==='Loading'?'History indexing; full market view is loading.':'')}
function render(d){state=d;let g=d.guard||{},p=d.policy||{},m=g.measurements||{},sy=d.symbols||[],base=sy.filter(x=>x.baseline_ready===true).length;let ageSeconds=g.reference_ts?seconds(g.reference_ts):g.reference_age_s;let mainReason=g.main_reason||'No decision';let ms=[['Reference market',prettyState(g.market_state||d.clock?.state),'Reference age '+age(ageSeconds)],['Allowed size','$'+fmt(g.allowed_notional),'Requested $'+fmt(g.requested_notional)],['Why this result',mainReason,statusLabel(g.status||'UNKNOWN')],['Data coverage',sy.length?base+'/'+sy.length:'Pending','Regular-hours minimum '+fmt(p.min_rth_samples,0)+'/token'],['Orders',p.executor_enabled===true?'On':'Off',p.executor_enabled===true?'Order path connected':'This page checks requests only'],['Latest decision',g.receipt_seq?'#'+g.receipt_seq:'Pending',time(g.ts)]];let tones=['cyan','violet','red','mint','amber','pink'];$('metrics').innerHTML=ms.map((x,i)=>'<article class="metric" style="--accent:var(--'+tones[i]+')"><div class="label">'+esc(x[0])+'</div><div class="value">'+esc(x[1])+'</div><div class="note">'+esc(x[2])+'</div></article>').join('');$('decisiontag').innerHTML=badge(g.status||'UNKNOWN');$('decision').innerHTML='<div class="value">$'+fmt(g.allowed_notional)+'</div><p>'+cleanText(g.rationale||g.note,g)+'</p><div class="note">Recorded '+esc(time(g.ts))+'</div>';
let entries=g.checks||[];$('checks').innerHTML=entries.map((entry,i)=>'<div class="check"><span class="dot" style="color:'+(['var(--cyan)','var(--mint)','var(--amber)','var(--violet)','var(--blue)','var(--pink)','var(--red)'][i]||'var(--muted)')+'"></span><b>'+esc(entry.name)+'</b>'+badge(entry.status)+'<details><summary>What was found</summary>'+esc(entry.detail||'No recorded detail')+'</details></div>').join('')||'<div class="note">No recorded checks.</div>';
let receipt=(d.receipts||[]).find(r=>r.seq===g.receipt_seq),factors=receipt?.check_factors||[];$('sizing').innerHTML=factors.map(item=>{let f=item.factor;return '<div class="fact"><div class="row"><span>'+esc(item.name)+'</span><span>'+ (f==null?'Not available':fmt(f,3)+'× · $'+fmt(Math.min(g.requested_notional,p.base_notional*f)))+'</span></div>'+(f==null?'':'<div class="bar"><i style="width:'+Math.max(0,Math.min(100,f*100))+'%"></i></div>')+'</div>'}).join('')+'<p class="note">The allowed amount is the smallest safe ceiling. One blocking result sets it to zero.</p>';
let missing=m.exposure_check==='required account report missing'||m.exposure_check==='not provided';$('exposure').innerHTML='<div class="facts">'+fact('Account evidence',prettyMeasurement(m.exposure_check||'Not provided'))+fact('Gross allowance','$'+fmt(p.max_gross_usdt))+fact('Recent account report',p.require_snapshot===true?'Required before adding exposure':'Not required')+fact('Position view',missing?'Not provided':'Available')+'</div><div class="callout"><strong>'+ (missing?'Why the allowed amount is zero':'Account exposure is included')+'</strong>'+ (missing?'Before adding exposure, AFTERBELL needs a recent, signed account position report. This decision did not include one, so the system refuses to guess how much is already held.':'The decision included a recent, signed account position report, so current holdings can be included in the exposure calculation.')+'</div><p class="note">The report is signed outside this console; this console never stores an account credential.</p>';
let po=d.posture||{},bands=po.bands||{},ch=po.changes||[];$('posture').innerHTML='<div class="facts">'+Object.entries({'Reference market':bands.market_state,'Independent price':bands.reference,'Liquidity':bands.liquidity,'Price agreement':bands.price_agreement,'Venue status':bands.venue_status,'Account evidence':bands.account_evidence}).map(([k,v])=>fact(k,v||'—')).join('')+'</div>'+(ch.length?ch.map(c=>'<details class="receipt"><summary><b>'+esc(c.symbol)+'</b><span class="note">#'+esc(c.seq)+' · '+esc(time(c.ts))+' · '+(c.changed||[]).length+' band(s) moved</span></summary>'+(c.changed||[]).map(m=>'<div class="note">'+esc(m.band)+': '+esc(m.before)+' → '+esc(m.after)+'</div>').join('')+'<p class="note">'+esc(c.explanation||'')+'</p></details>').join(''):'<p class="note">No material change recorded yet. Most cycles change nothing, which is the point: this records only a move in a band the limits already respond to.</p>')+'<p class="note">Nobody requests these. The monitor records a change on its own and authorises nothing by doing so.</p>';
$('markets').innerHTML=sy.map(s=>'<tr><td><b>'+esc(s.symbol)+'</b><div class="note">'+esc(s.underlying)+'</div></td><td>'+badge(s.status)+'</td><td>'+fmt(s.mid)+'</td><td>'+fmt(s.reference_price)+'</td><td>'+fmt(s.basis_bps,1)+'</td><td>'+fmt(s.half_spread_bps,3)+'</td><td>$'+fmt(s.depth_1pct,0)+'</td><td>'+fmt(s.walk_5k_bps,1)+'</td><td>'+fmt(s.spread_ratio)+'×</td><td>'+fmt(s.liquidity_ratio==null?null:s.liquidity_ratio*100,0)+'%</td><td>'+fmt(s.n_rth,0)+' / '+fmt(p.min_rth_samples,0)+'</td></tr>').join('');let coverageComplete=sy.length&&base===sy.length;$('baselineflag').innerHTML=badge(coverageComplete?'Complete':'Not ready');$('baselines').innerHTML=sy.map(s=>fact(s.asset,fmt(s.n_rth,0)+' regular-hours samples · '+fmt(s.holiday_samples,0)+' holiday samples')).join('')+'<p class="note">This shows whether the recorded data is deep enough to compare today’s market with its normal conditions.</p>';
$('policy').innerHTML='<div class="value">$'+fmt(p.base_notional)+'</div><p>Current maximum for one request. Change this amount in <code>config/policy.yaml</code> to configure the limit. The checks below may reduce it when market conditions call for caution.</p><a href="https://github.com/Jennycruzy/afterbell/blob/main/config/policy.yaml">Configure the order limit ↗</a>';
let b=d.backup||{},bst=b.status==='Healthy'&&seconds(b.ts)>7200?'Stale':b.status||'Unknown';$('backuptag').innerHTML=badge(bst);$('operations').innerHTML=fact('Verified through',b.covered_through)+fact('Last backup',time(b.ts))+fact('Retention',b.retention_days?b.retention_days+' days':'Unknown')+'<p class="note">Stable-file verification. The active raw archive is excluded from verification. A status older than two hours is considered stale.</p>';
let ev=d.evaluation||{};$('evidence').innerHTML=Object.entries(ev).filter(([k])=>/Adversarial attacks|Payloads that raised|False-positive|Positive controls/.test(k)).map(([k,v])=>fact(k,v)).join('')+'<p class="note">The test suite includes both hostile requests and ordinary market conditions, so it cannot pass by refusing everything.</p>';let a=d.acceptance||{};$('acceptance').innerHTML=fact('Historical order',a.order_id)+fact('Quantity',a.quantity)+fact('Notional',a.notional)+'<p class="note">This is a recorded acceptance event, not an open position. The account credential stayed with the supported client.</p>';
$('calreport').textContent=cleanText(d.calibration_markdown||'Report unavailable');$('hashes').textContent='Order-limit settings '+(p.sha256||'Unknown')+' · Registry '+(d.registry_sha256||'Unknown')+' · History '+(d.ledger_head||'Unknown');let prev=$('symbol').value;$('symbol').innerHTML=['All',...new Set((d.receipts||[]).map(r=>r.symbol))].map(s=>'<option>'+esc(s)+'</option>').join('');if([...$('symbol').options].some(o=>o.value===prev))$('symbol').value=prev;draw();receipts();freshness()}
$('inspect').onclick=()=>{selected=matching()[0]||null;$('inspectresult').textContent=selected?'Decision #'+selected.seq+' · '+cleanText(selected.rationale):'No matching decision available.'};$('download').onclick=()=>{let r=selected||matching()[0];if(!r)return;let url=URL.createObjectURL(new Blob([JSON.stringify(r,null,2)],{type:'application/json'})),a=document.createElement('a');a.href=url;a.download='afterbell-decision-'+r.seq+'.json';a.click();URL.revokeObjectURL(url)};$('symbol').onchange=$('outcome').onchange=()=>{selected=null;receipts()};document.querySelectorAll('[data-range]').forEach(b=>b.onclick=()=>{range=Number(b.dataset.range);document.querySelectorAll('[data-range]').forEach(x=>x.classList.toggle('active',x===b));if(state)draw()});async function full(){try{let r=await fetch('/api/state',{cache:'no-store',signal:AbortSignal.timeout(15000)});if(!r.ok)throw Error('HTTP '+r.status);render(await r.json())}catch(e){$('connection').textContent='DISCONNECTED';$('connection').className='tag status stale';$('freshness').textContent='Refresh failed. Displayed values are retained observations; check their timestamps.'}}async function tick(){if(!state)return full();try{let r=await fetch('/api/status',{cache:'no-store',signal:AbortSignal.timeout(5000)});if(!r.ok)throw Error('HTTP '+r.status);let d=await r.json();state={...state,...d};render(state)}catch(e){$('connection').textContent='DISCONNECTED';$('connection').className='tag status stale'}}full();setInterval(tick,10000);setInterval(full,300000);setInterval(freshness,1000)})();
</script></body></html>"""
